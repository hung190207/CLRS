# Exercise 3.1-4

Is $2^{n+1} = O(2^n)? \ $ 
Is $2^{2n} = O(2^n)?$

# Answer

1. $2^{n + 1} = 2 * 2^n$

For all $n$,

$$
2^{n+1} \le 2 * 2^n.
$$

So by the definition of Big-O, the problem is true.

2. $2^{2n} = O(2^n)$

$$
2^{2n} = 2^n * 2^n \le O(2^n)
$$

In fact, $2^{2n} = \Theta(4^n)$.

